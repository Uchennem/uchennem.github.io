import SwapiHelper from "./SwapiHelper.js";

const myHelper = new SwapiHelper("output", "films", "loading");

// myHelper.init();

function size(a) {
    return size.length
    }
    console.log(size(['a', 'b'])); //should return 2
